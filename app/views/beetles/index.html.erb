<div class="container">

<% if user_signed_in? %>
  <h5> <strong><p> Hi, <%= current_user.email %>. </p></strong></h5>
<% end %>
<p>
</p>
<h3>All about Beetles</h3>

<p>
The Volkswagen Beetle or informally the Volkswagen Bug, is a two-door, four passenger, rear-engined economy car manufactured and marketed by German automaker Volkswagen (VW) from 1938.
</p>
<br>
<% if user_signed_in? %>

  <p> CREATE a <%= link_to 'New Beetle', new_beetle_path %> linked to your profile. </p>

<% end %>

<br>

<h4>Beetles Listing</h4>
<table class="table">

  <thead>
    <tr>
      <th>User</th>
      <th>Image</th>
      <th>Nickname</th>
      <th>Address</th>
      <th>Year</th>
      <th>Color</th>
      <th>Engine</th>
      <th>Interior</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>

    <% @beetles.each do |beetle| %>

      <tr>
        <td><%= beetle.user.email if beetle.user %></td>
        <td><%= image_tag beetle.image.url(:medium) %></td>
        <td><%= beetle.nickname %></td>
        <td><%= beetle.address %></td>
        <td><%= beetle.year %></td>
        <td><%= beetle.color %></td>
        <td><%= beetle.engine %></td>
        <td><%= beetle.interior %></td>

        <td><%= link_to 'Show', beetle %></td>

        <% if beetle.user == current_user %>
          <td><%= link_to 'Edit', edit_beetle_path(beetle) %></td>
          <td><%= link_to 'Destroy', beetle, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>

    <% end %>
  </tbody>
</table>

</div>




